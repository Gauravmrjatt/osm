import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "OSM - Offers & Cashback Platform",
  description: "Get the best cashback deals and offers on top brands. Save money with exclusive coupons and promo codes.",
  keywords: "cashback, offers, deals, coupons, promo codes, discounts, savings",
  openGraph: {
    title: "OSM - Offers & Cashback Platform",
    description: "Get the best cashback deals and offers on top brands",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
